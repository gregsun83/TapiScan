import { HotelsContent } from "@/components/pages/hotels-content";

export const metadata = {
  title: "Hotels | TapiScan",
  description:
    "NFC and QR smart access for hotels — room info, service zones, poolside, lounge, guest information, and service requests without an app."
};

export default function HotelsPage() {
  return <HotelsContent />;
}
